---
const menuItems = [
  { text: "Home", href: "#" },
  { text: "About", href: "#about" },
  { text: "Properties", href: "#properties" },
  { text: "Contact", href: "#contact" }
];

const testimonials = [
  { name: "John Doe", text: "This is the best real estate service I've ever used!", image: "/images/john.jpg" },
  { name: "Jane Smith", text: "Highly recommend! They made finding my dream home so easy.", image: "/images/jane.jpg" }
];

const pricingPlans = [
  { title: "Basic", price: "$99/month", features: ["1 Property Listing", "Basic Support"] },
  { title: "Pro", price: "$199/month", features: ["5 Property Listings", "Priority Support"] },
  { title: "Enterprise", price: "$499/month", features: ["Unlimited Listings", "24/7 Support"] }
];
---

<nav class="bg-white shadow-md">
  <div class="container mx-auto px-4 py-4 flex justify-between items-center">
    <div class="text-2xl font-bold">RealEstate</div>
    <ul class="flex space-x-4">
      {menuItems.map(item => (
        <li><a href={item.href} class="text-gray-700 hover:text-blue-500">{item.text}</a></li>
      ))}
    </ul>
  </div>
</nav>

<section class="bg-blue-500 text-white py-20">
  <div class="container mx-auto px-4 text-center">
    <h1 class="text-4xl font-bold mb-4">Find Your Dream Home</h1>
    <p class="text-lg mb-8">Discover the perfect property with our expert real estate services.</p>
    <a href="#properties" class="bg-white text-blue-500 px-6 py-3 rounded-full font-semibold hover:bg-gray-100">Explore Properties</a>
  </div>
</section>

<section id="testimonials" class="py-20">
  <div class="container mx-auto px-4">
    <h2 class="text-3xl font-bold text-center mb-12">What Our Clients Say</h2>
    <div class="grid grid-cols-1 md:grid-cols-2 gap-8">
      {testimonials.map(testimonial => (
        <div class="bg-white shadow-md rounded-lg p-6">
          <div class="flex items-center mb-4">
            <img src={testimonial.image} alt={testimonial.name} class="w-12 h-12 rounded-full mr-4" />
            <div>
              <h3 class="text-xl font-semibold">{testimonial.name}</h3>
            </div>
          </div>
          <p class="text-gray-700">{testimonial.text}</p>
        </div>
      ))}
    </div>
  </div>
</section>

<section id="pricing" class="bg-gray-100 py-20">
  <div class="container mx-auto px-4">
    <h2 class="text-3xl font-bold text-center mb-12">Pricing Plans</h2>
    <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
      {pricingPlans.map(plan => (
        <div class="bg-white shadow-md rounded-lg p-6 text-center">
          <h3 class="text-2xl font-bold mb-4">{plan.title}</h3>
          <p class="text-3xl font-semibold mb-4">{plan.price}</p>
          <ul class="text-gray-700 mb-6">
            {plan.features.map(feature => (
              <li class="mb-2">{feature}</li>
            ))}
          </ul>
          <a href="#contact" class="bg-blue-500 text-white px-6 py-3 rounded-full font-semibold hover:bg-blue-600">Get Started</a>
        </div>
      ))}
    </div>
  </div>
</section>

<footer class="bg-gray-800 text-white py-8">
  <div class="container mx-auto px-4 text-center">
    <p>&copy; 2023 RealEstate. All rights reserved.</p>
  </div>
</footer>